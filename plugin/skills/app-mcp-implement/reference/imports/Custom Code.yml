appSlug: Custom Code
slug: Custom Code
config:
  functions:
    isAgentAllowed:
      parameters:
        list:
          type: string
        agentId:
          type: string
      code: |
        const v = (list === null || list === undefined) ? '' : String(list);
        if (v.indexOf('{' + '{') !== -1) return { allowed: false, wildcard: false };
        const ids = v.split(',').map(function (s) { return s.trim(); }).filter(Boolean);
        // "*" = allow ALL agents (disables the per-agent gate).
        const wildcard = ids.indexOf('*') !== -1;
        return { allowed: wildcard || ids.indexOf(String(agentId)) !== -1, wildcard: wildcard };
    makeConfigRef:
      parameters:
        key:
          type: string
      code: |
        // Build the literal binding string "{{config.<key>}}" in plain JS so
        // DSUL never parses it as a token. Used by onInstall to write binding A
        // into the app instance config (single-pass substitution stores the
        // literal, which resolves later against the tenant workspace config).
        return '{' + '{config.' + key + '}' + '}';
    makeSecretRef:
      parameters:
        key:
          type: string
      code: |
        // Build the literal binding string "{{secret.<key>}}" in plain JS.
        // Used by onInstall to write binding B into the tenant workspace
        // config.value (the only place {{secret.x}} resolves).
        return '{' + '{secret.' + key + '}' + '}';
    makeTokenSecretName:
      parameters:
        userId:
          type: string
      code: |
        // Workspace-secret name holding a user's Salesforce OAuth refresh token,
        // used for non-interactive (cron / autonomous) resolution. Reject empty
        // or unresolved-binding ids so distinct users can never collapse onto the
        // same secret name (defense-in-depth).
        var u = (userId === null || userId === undefined) ? '' : String(userId).trim();
        if (!u || u.indexOf('{' + '{') >= 0) { return { error: 'invalid_user' }; }
        return { name: 'sfnRefresh_' + u };
    normalizeLoginHost:
      parameters:
        rawHost:
          type: string
      code: |
        // Single source of truth for the Salesforce host rule (used by every
        // OAuth/token flow). Salesforce OAuth/API endpoints live ONLY on
        // *.my.salesforce.com (or login/test.salesforce.com) — NEVER on the
        // Lightning UI host. Apply the default fallback + rewrite the Lightning
        // host (replace-all) so a pasted lightning.force.com URL still works.
        var h = (rawHost === null || rawHost === undefined || String(rawHost).trim() === '')
          ? 'https://login.salesforce.com'
          : String(rawHost).trim();
        return { host: h.split('.lightning.force.com').join('.my.salesforce.com') };
    buildQueryString:
      parameters:
        params:
          type: object
      code: |
        const p = params || {};
        const parts = [];
        for (const [k, v] of Object.entries(p)) {
          if (v === null || v === undefined || v === '') continue;
          if (Array.isArray(v)) {
            for (const item of v) parts.push(encodeURIComponent(k) + '=' + encodeURIComponent(String(item)));
          } else if (typeof v === 'object') {
            parts.push(encodeURIComponent(k) + '=' + encodeURIComponent(JSON.stringify(v)));
          } else {
            parts.push(encodeURIComponent(k) + '=' + encodeURIComponent(String(v)));
          }
        }
        return parts.length ? '?' + parts.join('&') : '';
    generatePkce:
      parameters: {}
      code: |
        const crypto = require('crypto');
        const codeVerifier = crypto.randomBytes(32).toString('base64url');
        const codeChallenge = crypto.createHash('sha256').update(codeVerifier).digest('base64url');
        return { codeVerifier, codeChallenge };
    generateState:
      parameters: {}
      code: |
        const crypto = require('crypto');
        return { state: crypto.randomBytes(16).toString('base64url') };
    buildAuthorizeUrl:
      parameters:
        authorizeUrl:
          type: string
        clientId:
          type: string
        redirectUri:
          type: string
        scopes:
          type: string
        state:
          type: string
        codeChallenge:
          type: string
      code: |
        // No URL/URLSearchParams in the sandbox — build the querystring manually
        const enc = encodeURIComponent;
        const qs = [
          'client_id=' + enc(clientId || ''),
          'redirect_uri=' + enc(redirectUri || ''),
          'response_type=code',
          'state=' + enc(state || ''),
          'scope=' + enc(scopes || 'api refresh_token offline_access'),
          'code_challenge=' + enc(codeChallenge || ''),
          'code_challenge_method=S256'
        ].join('&');
        return { url: (authorizeUrl || '') + '?' + qs };
    buildJwtAssertion:
      parameters:
        clientId:
          type: string
        username:
          type: string
        audience:
          type: string
        privateKey:
          type: string
      code: |
        const crypto = require('crypto');
        try {
          const header = { alg: 'RS256', typ: 'JWT' };
          const now = Math.floor(Date.now() / 1000);
          const claims = {
            iss: clientId,
            sub: username,
            aud: audience,
            exp: now + 300
          };
          const h = Buffer.from(JSON.stringify(header)).toString('base64url');
          const c = Buffer.from(JSON.stringify(claims)).toString('base64url');
          const signingInput = h + '.' + c;
          const signer = crypto.createSign('RSA-SHA256');
          signer.update(signingInput);
          signer.end();
          const sig = signer.sign(privateKey).toString('base64url');
          return { assertion: signingInput + '.' + sig };
        } catch (e) {
          return { error: (e && e.message) || 'JWT signing failed' };
        }
    mcpRegistry:
      parameters:
        mode:
          type: string
        entity:
          type: string
        action:
          type: string
        operationName:
          type: string
        args:
          type: object
        ctx:
          type: object
      code: |
        // <<GENERATED>> single source of truth — entity/action surface, REST specs and the
        // tools/list manifest all derive from REGISTRY below. Never hand-edit
        // one of the derived forms.
        //
        // modes:
        //   tools     -> { tools: [...] }        manifest served by tools/list (via listTools)
        //   resolve   -> { operationName }       entity + action -> operationName
        //   operation -> { method, path, ... }   operationName -> REST spec
        //   request   -> { method, path, query, body? }  built call for executeApiCall
        const REGISTRY = {
          "versions": {
            "blurb": "Discovery — REST API versions, available resources, org limits and recently-viewed items.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on versions."
            },
            "required": [],
            "params": {
              "limit": {
                "type": "number",
                "description": "Query parameter `limit`."
              }
            },
            "ops": {
              "getLimits": {
                "op": "getLimits",
                "method": "GET",
                "path": "/limits",
                "summary": "Get org limits (DailyApiRequests, DataStorageMB, ...)"
              },
              "getRecent": {
                "op": "getRecentItems",
                "method": "GET",
                "path": "/recent",
                "queryParams": [
                  "limit"
                ],
                "summary": "Most recently viewed items — params: query: limit"
              },
              "listResources": {
                "op": "listResources",
                "method": "GET",
                "path": "/resources",
                "summary": "List available resources for the current API version"
              },
              "listVersions": {
                "op": "listVersions",
                "method": "GET",
                "path": "/",
                "summary": "List available REST API versions"
              }
            },
            "outputFormat": true
          },
          "sobjects": {
            "blurb": "Salesforce sObject metadata — describe global (all objects) or per-object (fields, picklists, relationships).",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on sobjects."
            },
            "required": [],
            "params": {
              "sObjectName": {
                "type": "string",
                "description": "Salesforce path parameter `sObjectName`."
              }
            },
            "ops": {
              "describe": {
                "op": "describeSObject",
                "method": "GET",
                "path": "/sobjects/{sObjectName}/describe",
                "pathParams": [
                  "sObjectName"
                ],
                "summary": "Full describe of a single sObject (fields, picklists, child relationships) — params: path: sObjectName"
              },
              "describeBasic": {
                "op": "describeSObjectBasic",
                "method": "GET",
                "path": "/sobjects/{sObjectName}",
                "pathParams": [
                  "sObjectName"
                ],
                "summary": "Basic metadata for a single sObject (URLs + recent items) — params: path: sObjectName"
              },
              "describeGlobal": {
                "op": "describeGlobal",
                "method": "GET",
                "path": "/sobjects",
                "summary": "List all available sObjects and basic metadata for each"
              }
            },
            "outputFormat": true
          },
          "records": {
            "blurb": "CRUD on individual Salesforce records (Accounts, Contacts, Leads, Opportunities, …). One record per call.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on records."
            },
            "required": [],
            "params": {
              "sObjectName": {
                "type": "string",
                "description": "Salesforce path parameter `sObjectName`."
              },
              "recordId": {
                "type": "string",
                "description": "Salesforce path parameter `recordId`."
              },
              "fields": {
                "type": "string",
                "description": "Query parameter `fields`."
              },
              "externalIdField": {
                "type": "string",
                "description": "Salesforce path parameter `externalIdField`."
              },
              "externalIdValue": {
                "type": "string",
                "description": "Salesforce path parameter `externalIdValue`."
              },
              "fieldName": {
                "type": "string",
                "description": "Salesforce path parameter `fieldName`."
              },
              "start": {
                "type": "string",
                "description": "Query parameter `start`."
              },
              "end": {
                "type": "string",
                "description": "Query parameter `end`."
              },
              "body": {
                "type": "object",
                "additionalProperties": true,
                "description": "JSON request body. Required for actions: create, update, upsert."
              }
            },
            "ops": {
              "create": {
                "op": "createRecord",
                "method": "POST",
                "path": "/sobjects/{sObjectName}",
                "pathParams": [
                  "sObjectName"
                ],
                "bodyPassthrough": true,
                "summary": "Create a new record — params: path: sObjectName"
              },
              "delete": {
                "op": "deleteRecord",
                "method": "DELETE",
                "path": "/sobjects/{sObjectName}/{recordId}",
                "pathParams": [
                  "sObjectName",
                  "recordId"
                ],
                "summary": "Delete a record by ID — params: path: sObjectName, recordId"
              },
              "get": {
                "op": "getRecord",
                "method": "GET",
                "path": "/sobjects/{sObjectName}/{recordId}",
                "pathParams": [
                  "sObjectName",
                  "recordId"
                ],
                "queryParams": [
                  "fields"
                ],
                "summary": "Retrieve a single record by ID — params: path: sObjectName, recordId; query: fields"
              },
              "getDeleted": {
                "op": "getDeletedRecords",
                "method": "GET",
                "path": "/sobjects/{sObjectName}/deleted",
                "pathParams": [
                  "sObjectName"
                ],
                "queryParams": [
                  "start",
                  "end"
                ],
                "summary": "Records deleted within a time window (recycle bin) — params: path: sObjectName; query: start, end"
              },
              "getField": {
                "op": "getRecordField",
                "method": "GET",
                "path": "/sobjects/{sObjectName}/{recordId}/{fieldName}",
                "pathParams": [
                  "sObjectName",
                  "recordId",
                  "fieldName"
                ],
                "summary": "Get a single field on a record (incl. blob/document body) — params: path: sObjectName, recordId, fieldName"
              },
              "getUpdated": {
                "op": "getUpdatedRecords",
                "method": "GET",
                "path": "/sobjects/{sObjectName}/updated",
                "pathParams": [
                  "sObjectName"
                ],
                "queryParams": [
                  "start",
                  "end"
                ],
                "summary": "Records updated within a time window — params: path: sObjectName; query: start, end"
              },
              "update": {
                "op": "updateRecord",
                "method": "PATCH",
                "path": "/sobjects/{sObjectName}/{recordId}",
                "pathParams": [
                  "sObjectName",
                  "recordId"
                ],
                "bodyPassthrough": true,
                "summary": "Update fields on an existing record — params: path: sObjectName, recordId"
              },
              "upsert": {
                "op": "upsertRecord",
                "method": "PATCH",
                "path": "/sobjects/{sObjectName}/{externalIdField}/{externalIdValue}",
                "pathParams": [
                  "sObjectName",
                  "externalIdField",
                  "externalIdValue"
                ],
                "bodyPassthrough": true,
                "summary": "Upsert a record by external ID field — params: path: sObjectName, externalIdField, externalIdValue"
              }
            },
            "outputFormat": true
          },
          "query": {
            "blurb": "SOQL queries (database language), SOSL searches (cross-object text search), parameterized search and query pagination.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on query."
            },
            "required": [],
            "params": {
              "q": {
                "type": "string",
                "description": "Query parameter `q`."
              },
              "queryLocator": {
                "type": "string",
                "description": "Salesforce path parameter `queryLocator`."
              },
              "body": {
                "type": "object",
                "additionalProperties": true,
                "description": "JSON request body. Required for actions: parameterizedSearch."
              }
            },
            "ops": {
              "next": {
                "op": "getQueryNext",
                "method": "GET",
                "path": "/query/{queryLocator}",
                "pathParams": [
                  "queryLocator"
                ],
                "summary": "Retrieve the next batch of a paginated SOQL query — params: path: queryLocator"
              },
              "parameterizedSearch": {
                "op": "runParameterizedSearch",
                "method": "POST",
                "path": "/parameterizedSearch",
                "bodyPassthrough": true,
                "summary": "Structured parameterized search (richer than SOSL string)"
              },
              "soql": {
                "op": "runQuery",
                "method": "GET",
                "path": "/query",
                "queryParams": [
                  "q"
                ],
                "summary": "Run a SOQL query (excludes deleted/archived) — params: query: q"
              },
              "soqlAll": {
                "op": "runQueryAll",
                "method": "GET",
                "path": "/queryAll",
                "queryParams": [
                  "q"
                ],
                "summary": "Run a SOQL query including deleted/archived records — params: query: q"
              },
              "sosl": {
                "op": "runSearch",
                "method": "GET",
                "path": "/search",
                "queryParams": [
                  "q"
                ],
                "summary": "Run a SOSL search across multiple sObjects — params: query: q"
              }
            },
            "outputFormat": true
          },
          "composite": {
            "blurb": "Composite endpoints — execute up to 25 sub-requests in order (with refs), a batch (no refs), a graph (multiple in one transaction), or a parent+children tree insert.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on composite."
            },
            "required": [],
            "params": {
              "allOrNone": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `allOrNone` (shape varies per action)."
              },
              "collateSubrequests": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `collateSubrequests` (shape varies per action)."
              },
              "compositeRequest": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `compositeRequest` (shape varies per action)."
              },
              "haltOnError": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `haltOnError` (shape varies per action)."
              },
              "batchRequests": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `batchRequests` (shape varies per action)."
              },
              "graphs": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `graphs` (shape varies per action)."
              },
              "sObjectName": {
                "type": "string",
                "description": "Salesforce path parameter `sObjectName`."
              },
              "records": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `records` (shape varies per action)."
              }
            },
            "ops": {
              "batch": {
                "op": "runCompositeBatch",
                "method": "POST",
                "path": "/composite/batch",
                "bodyParams": [
                  "haltOnError",
                  "batchRequests"
                ],
                "summary": "Execute up to 25 independent sub-requests (no refs) — params: body: haltOnError, batchRequests"
              },
              "graph": {
                "op": "runCompositeGraph",
                "method": "POST",
                "path": "/composite/graph",
                "bodyParams": [
                  "graphs"
                ],
                "summary": "Execute multiple composite graphs in a single transaction — params: body: graphs"
              },
              "request": {
                "op": "runComposite",
                "method": "POST",
                "path": "/composite",
                "bodyParams": [
                  "allOrNone",
                  "collateSubrequests",
                  "compositeRequest"
                ],
                "summary": "Execute up to 25 sub-requests in order, with refs between them — params: body: allOrNone, collateSubrequests, compositeRequest"
              },
              "tree": {
                "op": "createSObjectTree",
                "method": "POST",
                "path": "/composite/tree/{sObjectName}",
                "pathParams": [
                  "sObjectName"
                ],
                "bodyParams": [
                  "records"
                ],
                "summary": "Insert a parent record and related children in a single transaction — params: path: sObjectName; body: records"
              }
            },
            "outputFormat": true
          },
          "compositeRecords": {
            "blurb": "Bulk records via the composite/sobjects collection — create/update/delete up to 200 records of mixed sObject types in a single call.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on compositeRecords."
            },
            "required": [],
            "params": {
              "ids": {
                "type": "string",
                "description": "Query parameter `ids`."
              },
              "fields": {
                "type": "string",
                "description": "Query parameter `fields`."
              },
              "allOrNone": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `allOrNone` (shape varies per action)."
              },
              "records": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `records` (shape varies per action)."
              },
              "sObjectName": {
                "type": "string",
                "description": "Salesforce path parameter `sObjectName`."
              },
              "externalIdField": {
                "type": "string",
                "description": "Salesforce path parameter `externalIdField`."
              }
            },
            "ops": {
              "createMany": {
                "op": "createRecords",
                "method": "POST",
                "path": "/composite/sobjects",
                "bodyParams": [
                  "allOrNone",
                  "records"
                ],
                "summary": "Create up to 200 records (mixed sObjects) in one call — params: body: allOrNone, records"
              },
              "createManyForType": {
                "op": "createRecordsForType",
                "method": "POST",
                "path": "/composite/sobjects/{sObjectName}",
                "pathParams": [
                  "sObjectName"
                ],
                "bodyParams": [
                  "allOrNone",
                  "records"
                ],
                "summary": "Create up to 200 records of the same sObject type — params: path: sObjectName; body: allOrNone, records"
              },
              "deleteMany": {
                "op": "deleteRecords",
                "method": "DELETE",
                "path": "/composite/sobjects",
                "queryParams": [
                  "ids",
                  "allOrNone"
                ],
                "summary": "Delete up to 200 records (mixed sObjects) in one call — params: query: ids, allOrNone"
              },
              "getMany": {
                "op": "getRecordsCollection",
                "method": "GET",
                "path": "/composite/sobjects/{sObjectName}",
                "pathParams": [
                  "sObjectName"
                ],
                "queryParams": [
                  "ids",
                  "fields"
                ],
                "summary": "Retrieve up to 2000 records of mixed sObjects in one call — params: query: ids, fields"
              },
              "updateMany": {
                "op": "updateRecords",
                "method": "PATCH",
                "path": "/composite/sobjects",
                "bodyParams": [
                  "allOrNone",
                  "records"
                ],
                "summary": "Update up to 200 records (mixed sObjects) in one call — params: body: allOrNone, records"
              },
              "upsertManyForType": {
                "op": "upsertRecordsForType",
                "method": "PATCH",
                "path": "/composite/sobjects/{sObjectName}/{externalIdField}",
                "pathParams": [
                  "sObjectName",
                  "externalIdField"
                ],
                "bodyParams": [
                  "allOrNone",
                  "records"
                ],
                "summary": "Upsert up to 200 records of the same sObject by external ID — params: path: sObjectName, externalIdField; body: allOrNone, records"
              }
            },
            "outputFormat": true
          },
          "bulkIngest": {
            "blurb": "Bulk API 2.0 ingest jobs — upload CSV data for insert/update/upsert/delete of large record sets. Asynchronous: poll `get` until state=JobComplete.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on bulkIngest."
            },
            "required": [],
            "params": {
              "isPkChunkingEnabled": {
                "type": "boolean",
                "description": "Query parameter `isPkChunkingEnabled`."
              },
              "jobType": {
                "type": "string",
                "description": "Query parameter `jobType`.",
                "enum": [
                  "BigObjectIngest",
                  "Classic",
                  "V2Ingest"
                ]
              },
              "queryLocator": {
                "type": "string",
                "description": "Query parameter `queryLocator`."
              },
              "object": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `object` (shape varies per action)."
              },
              "operation": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `operation` (shape varies per action)."
              },
              "externalIdFieldName": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `externalIdFieldName` (shape varies per action)."
              },
              "contentType": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `contentType` (shape varies per action)."
              },
              "lineEnding": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `lineEnding` (shape varies per action)."
              },
              "columnDelimiter": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `columnDelimiter` (shape varies per action)."
              },
              "assignmentRuleId": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `assignmentRuleId` (shape varies per action)."
              },
              "jobId": {
                "type": "string",
                "description": "Salesforce path parameter `jobId`."
              },
              "state": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `state` (shape varies per action)."
              },
              "rawBody": {
                "type": "string",
                "description": "Raw request body (CSV / other text). Content-Type: text/csv."
              }
            },
            "ops": {
              "create": {
                "op": "createIngestJob",
                "method": "POST",
                "path": "/jobs/ingest",
                "bodyParams": [
                  "object",
                  "operation",
                  "externalIdFieldName",
                  "contentType",
                  "lineEnding",
                  "columnDelimiter",
                  "assignmentRuleId"
                ],
                "summary": "Create a new bulk ingest job (operation + sObject + lineEnding + columnDelimiter) — params: body: object, operation, externalIdFieldName, contentType, lineEnding, columnDelimiter, assignmentRuleId"
              },
              "delete": {
                "op": "deleteIngestJob",
                "method": "DELETE",
                "path": "/jobs/ingest/{jobId}",
                "pathParams": [
                  "jobId"
                ],
                "summary": "Delete a bulk ingest job (only after JobComplete/Aborted/Failed) — params: path: jobId"
              },
              "get": {
                "op": "getIngestJob",
                "method": "GET",
                "path": "/jobs/ingest/{jobId}",
                "pathParams": [
                  "jobId"
                ],
                "summary": "Get bulk ingest job state and metrics — params: path: jobId"
              },
              "getFailed": {
                "op": "getIngestFailed",
                "method": "GET",
                "path": "/jobs/ingest/{jobId}/failedResults",
                "pathParams": [
                  "jobId"
                ],
                "summary": "Download CSV of failed rows with error messages — params: path: jobId"
              },
              "getSuccessful": {
                "op": "getIngestSuccessful",
                "method": "GET",
                "path": "/jobs/ingest/{jobId}/successfulResults",
                "pathParams": [
                  "jobId"
                ],
                "summary": "Download CSV of successfully processed rows — params: path: jobId"
              },
              "getUnprocessed": {
                "op": "getIngestUnprocessed",
                "method": "GET",
                "path": "/jobs/ingest/{jobId}/unprocessedRecords",
                "pathParams": [
                  "jobId"
                ],
                "summary": "Download CSV of rows not processed (job aborted before completion) — params: path: jobId"
              },
              "list": {
                "op": "listIngestJobs",
                "method": "GET",
                "path": "/jobs/ingest",
                "queryParams": [
                  "isPkChunkingEnabled",
                  "jobType",
                  "queryLocator"
                ],
                "summary": "List bulk ingest jobs — params: query: isPkChunkingEnabled, jobType, queryLocator"
              },
              "update": {
                "op": "updateIngestJob",
                "method": "PATCH",
                "path": "/jobs/ingest/{jobId}",
                "pathParams": [
                  "jobId"
                ],
                "bodyParams": [
                  "state"
                ],
                "summary": "Update job state (UploadComplete to start processing, Aborted to stop) — params: path: jobId; body: state"
              },
              "uploadData": {
                "op": "uploadIngestData",
                "method": "PUT",
                "path": "/jobs/ingest/{jobId}/batches",
                "pathParams": [
                  "jobId"
                ],
                "rawBodyParam": "rawBody",
                "contentType": "text/csv",
                "summary": "Upload CSV data to an Open ingest job — params: path: jobId; rawBody: rawBody (text/csv)"
              }
            },
            "outputFormat": true
          },
          "bulkQuery": {
            "blurb": "Bulk API 2.0 query jobs — run a SOQL query asynchronously and download CSV results (handles >2000 records that SOQL caps).",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on bulkQuery."
            },
            "required": [],
            "params": {
              "isPkChunkingEnabled": {
                "type": "boolean",
                "description": "Query parameter `isPkChunkingEnabled`."
              },
              "jobType": {
                "type": "string",
                "description": "Query parameter `jobType`.",
                "enum": [
                  "Classic",
                  "V2Query"
                ]
              },
              "concurrencyMode": {
                "type": "string",
                "description": "Query parameter `concurrencyMode`.",
                "enum": [
                  "Parallel",
                  "Serial"
                ]
              },
              "operation": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `operation` (shape varies per action)."
              },
              "query": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `query` (shape varies per action)."
              },
              "contentType": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `contentType` (shape varies per action)."
              },
              "lineEnding": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `lineEnding` (shape varies per action)."
              },
              "columnDelimiter": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `columnDelimiter` (shape varies per action)."
              },
              "jobId": {
                "type": "string",
                "description": "Salesforce path parameter `jobId`."
              },
              "state": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `state` (shape varies per action)."
              },
              "locator": {
                "type": "string",
                "description": "Query parameter `locator`."
              },
              "maxRecords": {
                "type": "number",
                "description": "Query parameter `maxRecords`."
              }
            },
            "ops": {
              "create": {
                "op": "createQueryJob",
                "method": "POST",
                "path": "/jobs/query",
                "bodyParams": [
                  "operation",
                  "query",
                  "contentType",
                  "lineEnding",
                  "columnDelimiter"
                ],
                "summary": "Create a bulk SOQL query job (returns jobId for polling) — params: body: operation, query, contentType, lineEnding, columnDelimiter"
              },
              "delete": {
                "op": "deleteQueryJob",
                "method": "DELETE",
                "path": "/jobs/query/{jobId}",
                "pathParams": [
                  "jobId"
                ],
                "summary": "Delete a bulk query job (only after completion/abort) — params: path: jobId"
              },
              "get": {
                "op": "getQueryJob",
                "method": "GET",
                "path": "/jobs/query/{jobId}",
                "pathParams": [
                  "jobId"
                ],
                "summary": "Get bulk query job state and metrics — params: path: jobId"
              },
              "getResults": {
                "op": "getQueryJobResults",
                "method": "GET",
                "path": "/jobs/query/{jobId}/results",
                "pathParams": [
                  "jobId"
                ],
                "queryParams": [
                  "locator",
                  "maxRecords"
                ],
                "summary": "Download CSV results of a completed bulk query job — params: path: jobId; query: locator, maxRecords"
              },
              "list": {
                "op": "listQueryJobs",
                "method": "GET",
                "path": "/jobs/query",
                "queryParams": [
                  "isPkChunkingEnabled",
                  "jobType",
                  "concurrencyMode"
                ],
                "summary": "List bulk query jobs — params: query: isPkChunkingEnabled, jobType, concurrencyMode"
              },
              "update": {
                "op": "updateQueryJob",
                "method": "PATCH",
                "path": "/jobs/query/{jobId}",
                "pathParams": [
                  "jobId"
                ],
                "bodyParams": [
                  "state"
                ],
                "summary": "Update query job state (Aborted only) — params: path: jobId; body: state"
              }
            },
            "outputFormat": true
          },
          "tooling": {
            "blurb": "Tooling API — query/CRUD metadata (ApexClass, CustomField, FlexiPage, …), run Apex synchronously or asynchronously, execute anonymous Apex, code completions.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on tooling."
            },
            "required": [],
            "params": {
              "sObjectName": {
                "type": "string",
                "description": "Salesforce path parameter `sObjectName`."
              },
              "q": {
                "type": "string",
                "description": "Query parameter `q`."
              },
              "anonymousBody": {
                "type": "string",
                "description": "Query parameter `anonymousBody`."
              },
              "classNames": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `classNames` (shape varies per action)."
              },
              "classids": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `classids` (shape varies per action)."
              },
              "suiteNames": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `suiteNames` (shape varies per action)."
              },
              "suiteids": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `suiteids` (shape varies per action)."
              },
              "maxFailedTests": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `maxFailedTests` (shape varies per action)."
              },
              "testLevel": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `testLevel` (shape varies per action)."
              },
              "tests": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `tests` (shape varies per action)."
              },
              "type": {
                "type": "string",
                "description": "Query parameter `type`.",
                "enum": [
                  "apex",
                  "visualforce"
                ]
              },
              "recordId": {
                "type": "string",
                "description": "Salesforce path parameter `recordId`."
              },
              "body": {
                "type": "object",
                "additionalProperties": true,
                "description": "JSON request body. Required for actions: createRecord, updateRecord."
              }
            },
            "ops": {
              "completions": {
                "op": "getApexCompletions",
                "method": "GET",
                "path": "/tooling/completions",
                "queryParams": [
                  "type"
                ],
                "summary": "Apex / VisualForce code completions — params: query: type"
              },
              "createRecord": {
                "op": "createToolingRecord",
                "method": "POST",
                "path": "/tooling/sobjects/{sObjectName}",
                "pathParams": [
                  "sObjectName"
                ],
                "bodyPassthrough": true,
                "summary": "Create a Tooling API record (e.g. ApexClass, CustomField) — params: path: sObjectName"
              },
              "deleteRecord": {
                "op": "deleteToolingRecord",
                "method": "DELETE",
                "path": "/tooling/sobjects/{sObjectName}/{recordId}",
                "pathParams": [
                  "sObjectName",
                  "recordId"
                ],
                "summary": "Delete a Tooling API record — params: path: sObjectName, recordId"
              },
              "describeGlobal": {
                "op": "describeToolingGlobal",
                "method": "GET",
                "path": "/tooling/sobjects",
                "summary": "List Tooling API sObjects"
              },
              "describeSObject": {
                "op": "describeToolingSObject",
                "method": "GET",
                "path": "/tooling/sobjects/{sObjectName}/describe",
                "pathParams": [
                  "sObjectName"
                ],
                "summary": "Describe a Tooling API sObject — params: path: sObjectName"
              },
              "executeAnonymous": {
                "op": "executeAnonymousApex",
                "method": "GET",
                "path": "/tooling/executeAnonymous",
                "queryParams": [
                  "anonymousBody"
                ],
                "summary": "Run a one-off Apex code block as the current user — params: query: anonymousBody"
              },
              "getRecord": {
                "op": "getToolingRecord",
                "method": "GET",
                "path": "/tooling/sobjects/{sObjectName}/{recordId}",
                "pathParams": [
                  "sObjectName",
                  "recordId"
                ],
                "summary": "Retrieve a Tooling API record — params: path: sObjectName, recordId"
              },
              "query": {
                "op": "runToolingQuery",
                "method": "GET",
                "path": "/tooling/query",
                "queryParams": [
                  "q"
                ],
                "summary": "Run a Tooling SOQL query (CustomField, ApexClass, FlexiPage, ...) — params: query: q"
              },
              "runTestsAsync": {
                "op": "runApexTestsAsync",
                "method": "POST",
                "path": "/tooling/runTestsAsynchronous",
                "bodyParams": [
                  "classNames",
                  "classids",
                  "suiteNames",
                  "suiteids",
                  "maxFailedTests",
                  "testLevel",
                  "tests"
                ],
                "summary": "Schedule async Apex test execution — params: body: classNames, classids, suiteNames, suiteids, maxFailedTests, testLevel, tests"
              },
              "runTestsSync": {
                "op": "runApexTestsSync",
                "method": "POST",
                "path": "/tooling/runTestsSynchronous",
                "bodyParams": [
                  "tests"
                ],
                "summary": "Run Apex tests synchronously (small scope only) — params: body: tests"
              },
              "updateRecord": {
                "op": "updateToolingRecord",
                "method": "PATCH",
                "path": "/tooling/sobjects/{sObjectName}/{recordId}",
                "pathParams": [
                  "sObjectName",
                  "recordId"
                ],
                "bodyPassthrough": true,
                "summary": "Update a Tooling API record — params: path: sObjectName, recordId"
              }
            },
            "outputFormat": true
          },
          "process": {
            "blurb": "Process Rules and Approval Processes — list, trigger, submit records for approval.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on process."
            },
            "required": [],
            "params": {
              "requests": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `requests` (shape varies per action)."
              },
              "sObjectName": {
                "type": "string",
                "description": "Salesforce path parameter `sObjectName`."
              },
              "ruleId": {
                "type": "string",
                "description": "Salesforce path parameter `ruleId`."
              },
              "contextIds": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `contextIds` (shape varies per action)."
              }
            },
            "ops": {
              "listApprovals": {
                "op": "listApprovals",
                "method": "GET",
                "path": "/process/approvals",
                "summary": "List approval requests visible to the current user"
              },
              "listRules": {
                "op": "listProcessRules",
                "method": "GET",
                "path": "/process/rules",
                "summary": "List active process rules across the org"
              },
              "listRulesForObject": {
                "op": "listProcessRulesForObject",
                "method": "GET",
                "path": "/process/rules/{sObjectName}",
                "pathParams": [
                  "sObjectName"
                ],
                "summary": "Process rules for a given sObject — params: path: sObjectName"
              },
              "submitApproval": {
                "op": "submitApproval",
                "method": "POST",
                "path": "/process/approvals",
                "bodyParams": [
                  "requests"
                ],
                "summary": "Submit a record into an approval process or act on an existing one — params: body: requests"
              },
              "triggerRule": {
                "op": "triggerProcessRule",
                "method": "POST",
                "path": "/process/rules/{sObjectName}/{ruleId}",
                "pathParams": [
                  "sObjectName",
                  "ruleId"
                ],
                "bodyParams": [
                  "contextIds"
                ],
                "summary": "Trigger a specific process rule against record IDs — params: path: sObjectName, ruleId; body: contextIds"
              }
            },
            "outputFormat": true
          },
          "quickActions": {
            "blurb": "Quick Actions — list/describe/invoke org-level quick actions (object-specific actions like LogACall, NewTask, …).",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on quickActions."
            },
            "required": [],
            "params": {
              "actionName": {
                "type": "string",
                "description": "Salesforce path parameter `actionName`."
              },
              "body": {
                "type": "object",
                "additionalProperties": true,
                "description": "JSON request body. Required for actions: invoke."
              }
            },
            "ops": {
              "describe": {
                "op": "describeQuickAction",
                "method": "GET",
                "path": "/quickActions/{actionName}",
                "pathParams": [
                  "actionName"
                ],
                "summary": "Describe a specific quick action — params: path: actionName"
              },
              "invoke": {
                "op": "invokeQuickAction",
                "method": "POST",
                "path": "/quickActions/{actionName}",
                "pathParams": [
                  "actionName"
                ],
                "bodyPassthrough": true,
                "summary": "Invoke a quick action with payload — params: path: actionName"
              },
              "list": {
                "op": "listQuickActions",
                "method": "GET",
                "path": "/quickActions",
                "summary": "Global quick actions"
              }
            },
            "outputFormat": true
          },
          "analytics": {
            "blurb": "Reports & Dashboards — list, describe, run reports/dashboards async, fetch async-instance results.",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on analytics."
            },
            "required": [],
            "params": {
              "reportId": {
                "type": "string",
                "description": "Salesforce path parameter `reportId`."
              },
              "includeDetails": {
                "type": "boolean",
                "description": "Query parameter `includeDetails`."
              },
              "instanceId": {
                "type": "string",
                "description": "Salesforce path parameter `instanceId`."
              },
              "dashboardId": {
                "type": "string",
                "description": "Salesforce path parameter `dashboardId`."
              },
              "body": {
                "type": "object",
                "additionalProperties": true,
                "description": "JSON request body. Required for actions: runReport."
              }
            },
            "ops": {
              "describeDashboard": {
                "op": "describeDashboard",
                "method": "GET",
                "path": "/analytics/dashboards/{dashboardId}",
                "pathParams": [
                  "dashboardId"
                ],
                "summary": "Describe a dashboard (components, filters, cache state) — params: path: dashboardId"
              },
              "describeReport": {
                "op": "describeReport",
                "method": "GET",
                "path": "/analytics/reports/{reportId}",
                "pathParams": [
                  "reportId"
                ],
                "queryParams": [
                  "includeDetails"
                ],
                "summary": "Describe a report (metadata + reportFactWithDetails when ?includeDetails=true) — params: path: reportId; query: includeDetails"
              },
              "getDashboardStatus": {
                "op": "getDashboardStatus",
                "method": "GET",
                "path": "/analytics/dashboards/{dashboardId}/status",
                "pathParams": [
                  "dashboardId"
                ],
                "summary": "Get the current refresh status of a dashboard — params: path: dashboardId"
              },
              "getReportInstance": {
                "op": "getReportInstance",
                "method": "GET",
                "path": "/analytics/reports/{reportId}/instances/{instanceId}",
                "pathParams": [
                  "reportId",
                  "instanceId"
                ],
                "summary": "Get the results of a specific report instance — params: path: reportId, instanceId"
              },
              "listDashboards": {
                "op": "listDashboards",
                "method": "GET",
                "path": "/analytics/dashboards",
                "summary": "List all dashboards accessible to the current user"
              },
              "listReportInstances": {
                "op": "listReportInstances",
                "method": "GET",
                "path": "/analytics/reports/{reportId}/instances",
                "pathParams": [
                  "reportId"
                ],
                "summary": "List async report instances for a report — params: path: reportId"
              },
              "listReports": {
                "op": "listReports",
                "method": "GET",
                "path": "/analytics/reports",
                "summary": "List all reports accessible to the current user"
              },
              "runDashboard": {
                "op": "runDashboard",
                "method": "POST",
                "path": "/analytics/dashboards/{dashboardId}",
                "pathParams": [
                  "dashboardId"
                ],
                "summary": "Refresh a dashboard (async) — params: path: dashboardId"
              },
              "runReport": {
                "op": "runReport",
                "method": "POST",
                "path": "/analytics/reports/{reportId}",
                "pathParams": [
                  "reportId"
                ],
                "queryParams": [
                  "includeDetails"
                ],
                "bodyPassthrough": true,
                "summary": "Execute a report asynchronously and return a report instance — params: path: reportId; query: includeDetails"
              }
            },
            "outputFormat": true
          },
          "metadata": {
            "blurb": "Metadata REST deploy — submit a zipped metadata package and poll the deploy status (recommended over SOAP Metadata API for new integrations).",
            "actionProp": {
              "type": "string",
              "description": "Operation to perform on metadata."
            },
            "required": [],
            "params": {
              "deployOptions": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `deployOptions` (shape varies per action)."
              },
              "file": {
                "type": "object",
                "additionalProperties": true,
                "description": "Body field `file` (shape varies per action)."
              },
              "requestId": {
                "type": "string",
                "description": "Salesforce path parameter `requestId`."
              },
              "includeDetails": {
                "type": "boolean",
                "description": "Query parameter `includeDetails`."
              }
            },
            "ops": {
              "deploy": {
                "op": "deployMetadata",
                "method": "POST",
                "path": "/metadata/deployRequest",
                "bodyParams": [
                  "deployOptions",
                  "file"
                ],
                "summary": "Deploy a zipped metadata package (Metadata REST) — params: body: deployOptions, file"
              },
              "getDeployStatus": {
                "op": "getMetadataDeployStatus",
                "method": "GET",
                "path": "/metadata/deployRequest/{requestId}",
                "pathParams": [
                  "requestId"
                ],
                "queryParams": [
                  "includeDetails"
                ],
                "summary": "Get status + details of a metadata deploy request — params: path: requestId; query: includeDetails"
              }
            },
            "outputFormat": true
          }
        };
        const ORPHAN_OPERATIONS = {};
        const OUTPUT_FORMAT = {"type": "string", "enum": ["structured", "verbose", "both"], "default": "verbose", "description": "Response format for the MCP client."};
        const QUERY_ALIASES = {};

        function operationsOf() {
          const all = Object.assign({}, ORPHAN_OPERATIONS);
          for (const ent of Object.keys(REGISTRY)) {
            const ops = REGISTRY[ent].ops;
            if (!ops) continue;  // plain tool, no REST operations
            for (const act of Object.keys(ops)) {
              const o = ops[act];
              if (o.virtual) continue;
              const spec = {};
              for (const k of Object.keys(o)) {
                if (k === 'op' || k === 'summary' || k === 'virtual') continue;
                spec[k] = o[k];
              }
              spec.pathParams = spec.pathParams || [];
              spec.queryParams = spec.queryParams || [];
              spec.bodyParams = spec.bodyParams || [];
              all[o.op] = spec;
            }
          }
          return all;
        }

        function buildTools(ctx) {
          const hidden = (ctx && ctx.hideTools) || [];
          const tools = [];
          for (const name of Object.keys(REGISTRY)) {
            if (hidden.indexOf(name) !== -1) continue;
            const e = REGISTRY[name];
            // Plain (non entity-grouped) tool — e.g. OAuth connect/disconnect. Served as-is;
            // hide it per-tenant with ctx.hideTools rather than post-filtering the response.
            if (e.plain) { tools.push(e.tool); continue; }
            const actions = Object.keys(e.ops).filter(function (a) {
              const skip = (ctx && ctx.hideActions && ctx.hideActions[name]) || [];
              return skip.indexOf(a) === -1;
            });
            if (!actions.length) continue;

            let description;
            if (e.descOverride) {
              description = e.descOverride;
            } else {
              const lines = [e.blurb, '', 'Available actions:'];
              for (const a of actions) {
                const o = e.ops[a];
                lines.push('- **' + a + '** (' + o.op + ')' + (o.summary ? ': ' + o.summary : ''));
              }
              if (e.trailer) lines.push('', e.trailer);
              description = lines.join('\n');
            }
            // Providers reject descriptions over 1024 chars (Gotcha 21). NOT truncated here:
            // cutting mid-sentence corrupts the action list the LLM relies on. Overruns
            // are flagged by the Phase 8 live audit so a human shortens the summaries.

            const actionProp = { type: 'string' };
            for (const k of Object.keys(e.actionProp || {})) actionProp[k] = e.actionProp[k];
            actionProp.enum = actions;

            const properties = { action: actionProp };
            for (const p of Object.keys(e.params || {})) properties[p] = e.params[p];
            if (e.outputFormat === true) properties.outputFormat = OUTPUT_FORMAT;
            else if (e.outputFormat) properties.outputFormat = e.outputFormat;

            const inputSchema = { type: 'object', required: ['action'].concat(e.required || []), properties: properties };
            for (const k of Object.keys(e.schemaExtra || {})) inputSchema[k] = e.schemaExtra[k];

            tools.push({ name: name, description: description, inputSchema: inputSchema });
          }
          return { tools: tools };
        }

        function resolve(entity, action) {
          const e = REGISTRY[entity];
          if (!e) return { error: 'Unknown entity: ' + entity + '. Available: ' + Object.keys(REGISTRY).sort().join(', ') };
          const avail = Object.keys(e.ops).sort().join(', ');
          if (!action) return { error: 'Missing required argument `action` for ' + entity + '. Available actions: ' + avail };
          const o = e.ops[action];
          if (!o) return { error: 'Unknown action `' + action + '` for entity `' + entity + '`. Available actions: ' + avail };
          return { operationName: o.op };
        }

        function getOperation(operationName) {
          const op = operationsOf()[operationName];
          if (!op) return { error: 'Unknown operation: ' + operationName };
          return op;
        }

        function buildRequest(operationName, args) {
          const op = operationsOf()[operationName];
          if (!op) return { error: 'Unknown operation: ' + operationName };
          const a = Object.assign({}, args || {});
          let path = op.path.startsWith('/') ? op.path.substring(1) : op.path;
          for (const placeholder of (op.pathParams || [])) {
            const value = a[placeholder];
            if (value === undefined || value === null || value === '') {
              return { error: 'Missing required path param: ' + placeholder };
            }
            const enc = (op.rawPathParams && op.rawPathParams.indexOf(placeholder) !== -1)
              ? String(value).split('/').map(function (s) { return encodeURIComponent(s); }).join('/')
              : encodeURIComponent(String(value));
            path = path.replace('{' + placeholder + '}', enc);
            delete a[placeholder];
          }
          const query = {};
          for (const k of (op.queryParams || [])) {
            if (a[k] !== undefined && a[k] !== null && a[k] !== '') {
              query[QUERY_ALIASES[k] || k] = a[k];
            }
          }
          const result = { method: op.method, path: path, query: query };
          if (op.baseUrl) result.baseUrl = op.baseUrl;
          if (op.host) result.host = op.host;
          if (op.rawBodyParam && a[op.rawBodyParam] !== undefined && a[op.rawBodyParam] !== null && a[op.rawBodyParam] !== '') {
            result.rawBody = a[op.rawBodyParam];
            result.contentType = op.contentType || 'application/octet-stream';
            return result;
          }
          if (op.bodyPassthrough) {
            if (a.body && typeof a.body === 'object' && Object.keys(a.body).length) {
              result.body = a.body;
              return result;
            }
            // Fallback: caller passed body fields at the top level instead of nesting them in `body`.
            const reserved = new Set(['action', 'outputFormat', 'body']);
            if (op.rawBodyParam) reserved.add(op.rawBodyParam);
            for (const k of (op.pathParams || [])) reserved.add(k);
            for (const k of (op.queryParams || [])) reserved.add(k);
            const fallback = {};
            for (const k of Object.keys(a)) {
              if (!reserved.has(k) && a[k] !== undefined && a[k] !== null && a[k] !== '') fallback[k] = a[k];
            }
            if (Object.keys(fallback).length) result.body = fallback;
            return result;
          }
          if ((op.bodyParams || []).length) {
            const body = {};
            for (const k of (op.bodyParams || [])) {
              if (a[k] !== undefined && a[k] !== null && a[k] !== '') body[k] = a[k];
            }
            if (Object.keys(body).length) result.body = body;
          }
          return result;
        }

        switch (mode) {
          case 'tools': return buildTools(ctx);
          case 'resolve': return resolve(entity, action);
          case 'operation': return getOperation(operationName);
          case 'request': return buildRequest(operationName, args);
          default: return { error: 'Unknown mode: ' + mode + '. Expected tools | resolve | operation | request.' };
        }
